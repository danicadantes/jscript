let height=10;
let base=4;

console.log(`The perimeter of a rectangle has two equal sides to the height and the other two sides are equal to the base. If the height is ${height} and the base is ${base}, therefore the perimeter is ${2*(height*base)}`);
console.log(`To find the area of a rectangle, multiply the height by the base which will give you ${height*base}`);